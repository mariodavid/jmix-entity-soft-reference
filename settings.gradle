rootProject.name = 'jmix-entity-soft-reference-addon'

include 'jmix-entity-soft-reference'
include 'jmix-entity-soft-reference-starter'

rootProject.children.each { p1 ->
    p1.buildFileName = "${p1.name}.gradle"
}
